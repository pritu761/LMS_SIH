import { DemoLoadingSkeleton } from '@/components/demo/DemoLoadingSkeleton';

/** Loading state for the stations map. */
export default function StationsLoading() {
  return <DemoLoadingSkeleton />;
}
